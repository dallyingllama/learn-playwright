# Test info

- Name: 📊 Web Tables >> ❌ Delete user from table and verify removal
- Location: /home/runner/work/learn-playwright/learn-playwright/tests/web-tables.spec.ts:38:7

# Error details

```
Error: locator.click: Test timeout of 30000ms exceeded.
Call log:
  - waiting for getByRole('table').getByRole('rowgroup').nth(1).getByRole('row').filter({ hasText: 'Howell' }).locator('[title="Delete"]')
    - locator resolved to <span title="Delete" id="delete-record-4" data-toggle="tooltip">…</span>
  - attempting click action
    2 × waiting for element to be visible, enabled and stable
      - element is visible, enabled and stable
      - scrolling into view if needed
      - done scrolling
      - <div id="age-wrapper" class="mt-2 row">…</div> from <div role="dialog" tabindex="-1" aria-modal="true" class="fade modal">…</div> subtree intercepts pointer events
    - retrying click action
    - waiting 20ms
    - waiting for element to be visible, enabled and stable
    - element is not stable
  - retrying click action
    - waiting 100ms
    - waiting for element to be visible, enabled and stable
    - element is visible, enabled and stable
    - scrolling into view if needed
    - done scrolling
    - <div class="col-12 mt-4 col-md-3 col-xl-3">…</div> intercepts pointer events
  - retrying click action
    - waiting 100ms
    - waiting for element to be visible, enabled and stable
    - element is not stable
  51 × retrying click action
       - waiting 500ms
       - waiting for element to be visible, enabled and stable
       - element is visible, enabled and stable
       - scrolling into view if needed
       - done scrolling
       - <div class="col-12 mt-4 col-md-3 col-xl-3">…</div> intercepts pointer events
  - retrying click action
    - waiting 500ms

    at WebTablesPage.deleteUserByName (/home/runner/work/learn-playwright/learn-playwright/page-objects/web-tables-page.ts:86:21)
    at /home/runner/work/learn-playwright/learn-playwright/tests/web-tables.spec.ts:49:27
    at /home/runner/work/learn-playwright/learn-playwright/tests/web-tables.spec.ts:46:5
```

# Page snapshot

```yaml
- banner:
  - link:
    - /url: https://demoqa.com
    - img
- img
- text: Elements
- img
- list:
  - listitem:
    - link "Text Box":
      - /url: /text-box
      - img
      - text: Text Box
  - listitem:
    - link "Check Box":
      - /url: /checkbox
      - img
      - text: Check Box
  - listitem:
    - link "Radio Button":
      - /url: /radio-button
      - img
      - text: Radio Button
  - listitem:
    - link "Web Tables":
      - /url: /webtables
      - img
      - text: Web Tables
  - listitem:
    - link "Buttons":
      - /url: /buttons
      - img
      - text: Buttons
  - listitem:
    - link "Links":
      - /url: /links
      - img
      - text: Links
  - listitem:
    - link "Broken Links - Images":
      - /url: /broken
      - img
      - text: Broken Links - Images
  - listitem:
    - link "Upload and Download":
      - /url: /upload-download
      - img
      - text: Upload and Download
  - listitem:
    - link "Dynamic Properties":
      - /url: /dynamic-properties
      - img
      - text: Dynamic Properties
- img
- text: Forms
- img
- img
- text: Alerts, Frame & Windows
- img
- img
- text: Widgets
- img
- img
- text: Interactions
- img
- img
- text: Book Store Application
- img
- heading "Web Tables" [level=1]
- button "Add"
- textbox "Type to search": Howell
- button:
  - img
- table:
  - rowgroup:
    - row "First Name Last Name Age Email Salary Department Action":
      - cell "First Name"
      - cell "Last Name"
      - cell "Age"
      - cell "Email"
      - cell "Salary"
      - cell "Department"
      - cell "Action"
  - rowgroup:
    - row "Howell Romaguera 20 Abdiel_Maggio@gmail.com 149863 Tools EditDelete":
      - cell "Howell"
      - cell "Romaguera"
      - cell "20"
      - cell "Abdiel_Maggio@gmail.com"
      - cell "149863"
      - cell "Tools"
      - cell "EditDelete":
        - img
        - img
- group:
  - button "First" [disabled]
  - button "Previous" [disabled]
  - button "Next" [disabled]
  - button "Last" [disabled]
- text: Page
- strong: 1 of 1
- combobox:
  - option "Show 10" [selected]
  - option "Show 20"
  - option "Show 30"
  - option "Show 40"
  - option "Show 50"
- iframe
- iframe
- contentinfo: © 2013-2026 TOOLSQA.COM | ALL RIGHTS RESERVED.
```

# Test source

```ts
   1 | // page-objects/web-tables-page.ts
   2 | import { Page, Locator, expect } from '@playwright/test';
   3 | import { BasePage } from './base-page';
   4 | import { createGotoWithVariants } from '../utils/gotoHelper';
   5 | import { NavigablePage } from './interfaces/navigable-page';
   6 |
   7 | const config = {
   8 |   menu: 'Elements',
   9 |   menuItem: 'Web Tables',
   10 |   url: '/webtables',
   11 |   header: 'Web Tables',
   12 | };
   13 |
   14 | export class WebTablesPage extends BasePage implements NavigablePage {
   15 |   readonly goto;
   16 |
   17 |   constructor(page: Page) {
   18 |     super(page);
   19 |     this.goto = createGotoWithVariants(
   20 |       async () => {
   21 |         await this.openSidebarFromHome(config.menu);
   22 |         await this.sidebarMenu.navigateTo(config.menuItem);
   23 |         await this.waitForPageReady();
   24 |       },
   25 |       async () => {
   26 |         await this.page.goto(config.url);
   27 |         await this.waitForPageReady();
   28 |       }
   29 |     );
   30 |   }
   31 |
   32 |   override async waitForPageReady(): Promise<void> {
   33 |     await expect(this.page.locator('h1')).toHaveText(config.header);
   34 |     await expect(this.searchBox).toBeVisible();
   35 |     await expect(this.addButton).toBeVisible();
   36 |   }
   37 |
   38 |   async assertOnPage(): Promise<void> {
   39 |     await expect(this.page).toHaveURL(config.url);
   40 |     await this.waitForPageReady();
   41 |   }    
   42 |
   43 |   // Locators
   44 |   private addButton = this.page.getByRole('button', { name: 'Add' });
   45 |   private firstNameInput = this.page.locator('#firstName');
   46 |   private lastNameInput = this.page.locator('#lastName');
   47 |   private emailInput = this.page.locator('#userEmail');
   48 |   private ageInput = this.page.locator('#age');
   49 |   private salaryInput = this.page.locator('#salary');
   50 |   private departmentInput = this.page.locator('#department');
   51 |   private submitButton = this.page.locator('#submit');
   52 |   private searchBox = this.page.locator('#searchBox');
   53 |   private table = this.page.getByRole('table');
   54 |   private paginationSelect = this.page.getByRole('combobox');
   55 |
   56 |   private getBodyRowGroup(): Locator {
   57 |     return this.table.getByRole('rowgroup').nth(1);
   58 |   }
   59 |
   60 |   private getBodyRows(): Locator {
   61 |     return this.getBodyRowGroup().getByRole('row');
   62 |   }
   63 |
   64 |   // Add user to table
   65 |   async addUser(user: Record<string, string>) {
   66 |     await this.addButton.click();
   67 |     await this.firstNameInput.fill(user.firstName);
   68 |     await this.lastNameInput.fill(user.lastName);
   69 |     await this.emailInput.fill(user.email);
   70 |     await this.ageInput.fill(user.age);
   71 |     await this.salaryInput.fill(user.salary);
   72 |     await this.departmentInput.fill(user.department);
   73 |     await this.submitButton.click();
   74 |   }
   75 |
   76 |   // Search by keyword
   77 |   async search(keyword: string) {
   78 |     await this.searchBox.fill(keyword);
   79 |     await expect(this.searchBox).toHaveValue(keyword);
   80 |   }
   81 |
   82 |   // Delete user by name
   83 |   async deleteUserByName(name: string) {
   84 |     const row = this.getBodyRows().filter({ hasText: name });
   85 |     const deleteBtn = row.locator('[title="Delete"]');
>  86 |     await deleteBtn.click();
      |                     ^ Error: locator.click: Test timeout of 30000ms exceeded.
   87 |   }
   88 |
   89 |   // Count visible rows
   90 |   async getVisibleRowCount(): Promise<number> {
   91 |     return (await this.getRowData()).length;
   92 |   }
   93 |
   94 |   async getFilledRowCount(): Promise<number> {
   95 |     return (await this.getRowData()).length;
   96 |   }
   97 |
   98 |   async getRowData(): Promise<string[]> {
   99 |     const rows = this.getBodyRows();
  100 |     const count = await rows.count();
  101 |     const data: string[] = [];
  102 |
  103 |     for (let i = 0; i < count; i++) {
  104 |       const row = rows.nth(i);
  105 |       const text = (await row.innerText()).trim();
  106 |
  107 |       if (text) {
  108 |         data.push(text);
  109 |       }
  110 |     }
  111 |
  112 |     return data;
  113 |   }
  114 |
  115 |   async getMatchingRowCount(keyword: string): Promise<number> {
  116 |     return await this.getBodyRows().filter({ hasText: keyword }).count();
  117 |   }
  118 |
  119 |   async expectMatchingRows(keyword: string): Promise<void> {
  120 |     await expect
  121 |       .poll(async () => this.getMatchingRowCount(keyword))
  122 |       .toBeGreaterThan(0);
  123 |   }
  124 |
  125 |   async expectNoMatchingRows(keyword: string): Promise<void> {
  126 |     await expect
  127 |       .poll(async () => this.getMatchingRowCount(keyword))
  128 |       .toBe(0);
  129 |   }
  130 |
  131 |   async editUserByName(name: string, updated: Record<string, string>) {
  132 |     const row = this.getBodyRows().filter({ hasText: name });
  133 |     const editBtn = row.locator('[title="Edit"]');
  134 |     await editBtn.click();
  135 |   
  136 |     await this.firstNameInput.fill(updated.firstName);
  137 |     await this.lastNameInput.fill(updated.lastName);
  138 |     await this.emailInput.fill(updated.email);
  139 |     await this.ageInput.fill(updated.age);
  140 |     await this.salaryInput.fill(updated.salary);
  141 |     await this.departmentInput.fill(updated.department);
  142 |     await this.submitButton.click();
  143 |   }
  144 |
  145 |   // Select pagination size
  146 |   async setPaginationSize(size: string) {
  147 |     await this.paginationSelect.selectOption(size);
  148 |   }
  149 |   
  150 |   async getColumnValues(index: number): Promise<string[]> {
  151 |     const rows = this.getBodyRows();
  152 |     const values: string[] = [];
  153 |
  154 |     for (let i = 0; i < await rows.count(); i++) {
  155 |       const cells = rows.nth(i).getByRole('cell');
  156 |       const cellText = await cells.nth(index).textContent();
  157 |       if (cellText && cellText.trim()) {
  158 |         values.push(cellText.trim());
  159 |       }
  160 |     }
  161 |
  162 |     return values;
  163 |   }
  164 | }
  165 |
```